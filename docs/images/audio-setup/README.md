# Audio Setup Screenshot Placeholders

Store the actual screenshots referenced in `docs/AUDIO_SETUP.md` in this directory once they are captured. Use descriptive filenames that match the placeholders (e.g., `macos-blackhole-download.png`).

